name: cafe-pos

services:
  postgres:
    image: postgres:17-alpine
    container_name: pos-cafe-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: cafe_pos
      POSTGRES_PASSWORD: cafe_pos_dev
      POSTGRES_DB: cafe_pos
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      # Runs only on first boot of an empty volume. Creates cafe_pos_test so the
      # integration suite works without a manual setup step.
      - ./sql/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cafe_pos -d cafe_pos"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
